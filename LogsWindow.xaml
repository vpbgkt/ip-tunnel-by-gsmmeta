<Window x:Class="IPTunnelGsmMeta.LogsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Application Logs" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Logs" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBox Grid.Row="1" x:Name="LogsTextBox"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="11"
                 Padding="10"
                 Background="{StaticResource CardBrush}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="COPY ALL" Click="CopyButton_Click"
                    Margin="0,0,10,0"/>
            <Button Content="CLEAR" Click="ClearButton_Click"
                    Margin="0,0,10,0"/>
            <Button Content="CLOSE" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
